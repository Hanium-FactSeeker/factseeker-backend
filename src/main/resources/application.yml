spring:
  datasource:
    url: jdbc:mysql://facto-user-rds.c9a4eek04eqi.ap-northeast-2.rds.amazonaws.com:3306/facto-user
    username: root
    password: factouserrds12
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
    open-in-view: false

logging.level:
  org.hibernate.SQL: debug
  org.hibernate.orm.jdbc.bind: trace
